#pragma once
#include "binary_tree.h"
#include <chrono>
#include <iostream>
#include <set>
#include <random>
using namespace std;
using namespace chrono;
void testTime(int N = 10000, int M = 1000, int range = 100);